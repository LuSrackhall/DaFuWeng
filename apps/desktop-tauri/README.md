# Desktop Tauri Shell

This directory reserves the desktop shell that will embed the shared web client.

Initial responsibilities:

- boot the web client in a desktop container
- expose desktop-only capabilities through adapters
- avoid any gameplay rule branching inside the shell